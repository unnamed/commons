rootProject.name = "commons"

// Include and pre-pend 'commons-' to every subproject
["error", "functional", "reflect", "validation"].forEach {
	include it
	findProject(":$it")?.name = "commons-$it"
}